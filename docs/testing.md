# Проверка версии 0.3.1

## Автоматические проверки

Команды выполняются из `C:\projects\codex-edt` в PowerShell:

```powershell
.\scripts\build.ps1
.\scripts\start-dev-edt.ps1 -Smoke
# Отдельно, с действующим входом и расходом лимитов Codex:
.\scripts\start-dev-edt.ps1 -Smoke -Live
```

Обычная сборка не вызывает OpenAI. Тестовый Java app-server запускается настоящим дочерним процессом и проверяет отправленные JSONL-запросы. Он находится только в test bundle и не попадает в p2.

| Набор | Проверки |
|---|---|
| AppServerClientTest | 12 прежних тестов: JSONL, Unicode, id correlation, handshake, ошибки, timeout, недоступный executable, запрет действий, закрытие заблокированного stdin, свои/чужие процессы |
| EditorBufferTest | Saved/dirty, пустой dirty, выбор окна большого документа, выделение без дублирования, UTF-16 |
| SessionFeaturesTest | displayName/default, reasoning и смена модели, названия/даты чатов, история, New/Resume, Stop, смена проекта, аккаунт, ошибка/reconnect |
| MarkdownTest | Абзацы, bold/italic/code, списки, ссылки, инертный HTML; логика не зависит от светлой/тёмной темы |
| CodexViewTest | Реальная View, фокус чата, ITextEditor, selection, dirty document против сохранённого файла |
| StreamingViewTest | Потоковые обновления через SWT, cwd, неизменность SHA-256, закрытие процесса |
| IdeViewTest | Native composer, модель/reasoning, Enter/Shift+Enter и Preferences, Stop, новый чат, UI resume и продолжение, dirty без сохранения |
| CodexLiveTest | Два opt-in сценария с настоящим Codex: saved selection и dirty buffer + selection + server history/resume |

Журналы: `.runtime/logs/maven-build.log`, `tests/io.github.zhumaniezov.codex.edt.tests/target/surefire-reports`, `.runtime/edt-smoke-<timestamp>/edt.log` и `result.txt`. Временные проекты, снимки UI, schema и артефакты игнорируются Git.

Сохранённый результат проверки версии 0.3 перед её ручной приёмкой:

- Maven/Tycho: **BUILD SUCCESS**, 35 tests, 0 failures, 0 errors, 2 skipped; **33 теста прошли**, два live-теста отключены в обычном запуске.
- p2: `verify-repository` подтвердил целостность metadata/artifacts, включая CommonMark.
- Полный EDT 2026.1.3.25: **PASS, tests=33, failures=0** в `.runtime/edt-smoke-20260911192125`.
- Настоящий Codex: **PASS, tests=2, failures=0** в `.runtime/edt-smoke-20260911192205`. Saved selection дал 43 обновления текста SWT. Dirty-сценарий подтвердил два отдельных ответа, приоритет буфера без выделения, затем выделение внутри процедуры `Тест`, server history/resume, прежний cwd и неизменность файлов.
- Отдельный `local-check.ps1 -SkipSmoke`: javac на активных библиотеках установленной EDT и штатный p2 Publisher завершились успешно.
- Native-компоновка проверена при **460 × 750 px**: Send и selectors помещаются, область ответа имеет высоту 256 px. Снимок: `tests/io.github.zhumaniezov.codex.edt.tests/target/codex-view-stage3.png`.

Итоговый Maven-прогон завершён **11.09.2026 в 19:21:25 (+05:00)**. Версия p2: **0.3.0.202609111420**. SHA-256 установочного ZIP:

```text
67BF67B44E586A551B497FDFA6696CCBE2B207564D6B38AC4F5F05E43C33047B
```

Во всех сценариях закрытия проверяется именно принадлежащий сессии Process и известные потомки. Отдельный тест подтверждает, что посторонний дочерний процесс остаётся жив. Сетевые тесты создают служебные чаты в истории Codex; их временные файловые проекты после проверки удаляются. Такие тестовые чаты не следует возобновлять после удаления каталога.

Автоматические UI-сценарии запускаются в настоящем EDT product, но используют стандартный Eclipse Text Editor в временном файловом проекте. Они подтверждают публичный document API и end-to-end через View, **не заменяя ручной тест настоящего BSL-редактора конфигурации EDT**. Внешний location берётся через IProject location URI; основной пользовательский проект и информационная база не используются.

## Ручные тесты приёмки

Перед началом закройте предыдущий development EDT и запустите `scripts/start-dev-edt.ps1`. Откройте тестовую конфигурацию, её BSL-модуль и Codex View. Нужен уже действующий вход в Codex. На панели постоянно должен быть индикатор «Только чтение».

| Тест | Действия | Ожидаемый результат |
|---|---|---|
| TC-01 — открыть Codex View | Window → Show View → Other → Codex; перетащить вправо | Видны чаты, короткий статус, composer; интерфейс не блокирует EDT |
| TC-02 — новый чат | После ответа ввести черновик и нажать «Новый чат» | История текущей панели и composer очищены; новый thread создаётся при первом Send; процесс тот же |
| TC-03 — реальный streaming | Написать вопрос и нажать ↑ / Enter | Текст появляется частями; статус «Codex работает…», кнопка ■ |
| TC-04 — остановить turn | Во время длинного ответа нажать ■; отправить новый вопрос | «Остановлено», процесс и thread сохраняются, следующий вопрос работает |
| TC-05 — выбрать модель | Выбрать другую запись списка моделей, отправить короткий вопрос | В UI displayName; в диагностике соответствующий model id; ответ получен |
| TC-06 — выбрать reasoning | Выбрать уровень; затем модель с другим набором уровней | Только возможности выбранной модели; совместимый уровень сохранён либо выбран её default |
| TC-07 — список предыдущих чатов | Проверить названия/даты, нажать «Просмотреть ещё» при наличии | Новая страница приходит с сервера, строки не дублируются |
| TC-08 — открыть старый thread | Нажать строку в «Чаты»; при наличии — «Предыдущие сообщения» | Загружена серверная история в хронологическом порядке |
| TC-09 — продолжить старый thread | Отправить вопрос в открытый старый чат | Thread id в tooltip не меняется; используется cwd этого чата. Другой активный проект вызывает понятную ошибку |
| TC-10 — saved BSL + selection | Сохранить модуль, выделить короткий код, спросить о нём | Учитывается выделение; полный сохранённый модуль не вкладывается автоматически |
| TC-11 — dirty BSL без Ctrl+S | Изменить модуль, оставить *, снять выделение, спросить о содержимом | Codex видит текущий несохранённый буфер; документ остаётся dirty |
| TC-12 — dirty BSL + selection | В несохранённой процедуре выделить вызов и спросить о контексте | Учитываются и диапазон выделения, и окружающая несохранённая процедура |
| TC-13 — disk отличается от buffer | Сохранённый файл пустой; в редакторе непустая процедура | Ответ основан на буфере с приоритетом над диском, файл на диске остаётся пустым |
| TC-14 — Markdown | Попросить bold, italic, inline code, список, fenced BSL и HTTPS-ссылку | Форматирование native; ссылка открывается только по клику. HTML — текст, file/javascript/data ссылки не выполняются |
| TC-15 — закрыть/открыть View | Закрыть панель после ответа и во время выполнения, открыть снова | Старый собственный app-server завершён, новая панель подключается; чаты доступны с сервера |
| TC-16 — crash / reconnect | В тестовой среде завершить только дочерний app-server данной View; нажать «Повторить подключение» | «Отключён»/понятная ошибка, EDT жива; новый процесс и новые запросы работают |
| TC-17 — read-only SHA-256 | Снять хеши файлов тестового проекта до запроса и после, не сохранять dirty editor | Наборы хешей равны; у редактора сохраняется *; автоматических изменений нет |

TC-01, TC-03, TC-06 и TC-14 повторите в **Light** и **Dark**: Window → Preferences → General → Appearance → Theme (при запросе EDT — перезапуск). Проверьте текст, код, ссылки, состояния disabled, ширину правой панели. Полная ручная визуальная приёмка обеих тем остаётся за автором; автоматические тесты проверяют общий renderer без заданных RGB-цветов.

Дополнительно: **⋯ → настройки** — выключить Enter-to-send, убедиться, что Enter добавляет строку и ↑ отправляет; включить обратно. Диагностика по умолчанию скрыта и доступна через tooltip статуса. **Аккаунт** показывает данные сервера. Выход проверяйте только осознанно: он завершает общую авторизацию Codex; в автоматических тестах Logout выполняется исключительно на fake server.

Для TC-16 не завершайте все процессы по имени. Если собственный PID не удаётся надёжно отличить, используйте автоматический тест `errorAndReconnectDoNotLeaveOldProcess`; чужие процессы Codex трогать нельзя.

## Обязательный dirty-editor тест

1. Откройте общий модуль конфигурации в настоящем BSL-редакторе EDT.
2. Сохранённый `Module.bsl` может оставаться пустым. Вставьте в редактор, **не сохраняя**:

```bsl
Процедура Тест()
    Сообщить("Привет");
КонецПроцедуры
```

3. Убедитесь, что на вкладке есть `*`. Снимите выделение, **не нажимайте Ctrl+S**.
4. В Codex спросите: **«Какой код сейчас находится в открытом модуле?»**.
5. В ответе должна быть именно процедура `Тест` с `Сообщить("Привет")`, а не только утверждение, что файл на диске пустой.
6. Выделите `Сообщить("Привет");`.
7. Спросите: **«Что делает выделенный код и в каком контексте он находится?»**.
8. Codex должен объяснить вызов внутри несохранённой процедуры. Звёздочка остаётся, файл не сохраняется.
9. Повторите с большим модулем: окно ограничено 48 000 символами. Codex должен знать, что это фрагмент, и не заявлять, что получил весь несохранённый модуль.

## SHA-256 без изменения проекта

В PowerShell задайте **фактический каталог тестового проекта**, показанный в tooltip диагностики. Перед первым хешированием дождитесь окончания фоновых операций EDT. Не используйте каталог всей workspace с `.metadata`: сама EDT меняет служебные файлы.

```powershell
$testProject = 'C:\путь\к\тестовому-проекту'
$before = @(Get-ChildItem -LiteralPath $testProject -Recurse -File |
    Sort-Object FullName | Get-FileHash -Algorithm SHA256 |
    ForEach-Object { $_.Path + ' ' + $_.Hash })
# Здесь выполнить запрос в Codex. Dirty-документ не сохранять.
$after = @(Get-ChildItem -LiteralPath $testProject -Recurse -File |
    Sort-Object FullName | Get-FileHash -Algorithm SHA256 |
    ForEach-Object { $_.Path + ' ' + $_.Hash })
Compare-Object $before $after
```

Пустой вывод означает равенство. Если EDT параллельно пересобирала проект, разберите отличающиеся пути; не приписывайте их автоматически агенту. Автоматические сценарии используют изолированный файловый проект и сравнивают все его файлы.

## Ограничения и диагностика

Версия 0.3 вручную принята автором: настоящая конфигурация, BSL-редактор, dirty buffer и selection, один thread для последовательных запросов, New/Resume, модель/reasoning, Stop, обе темы, Account, Preferences и Enter/Shift+Enter. Markdown тогда не считался проверенным; для него выделен TC-22.

UI установки ZIP в основную EDT, PDE Debug, другие версии Codex/EDT, сетевые и удалённые с диска каталоги, Compare Editor и редакторы без публичного ITextEditor не считаются проверенными этой сборкой. История показывает текст пользователя/ассистента; подробные tool items пока скрыты. Большой dirty-модуль передаётся окном, точный diff не вычисляется. HTTP(S)-ссылки поддерживаются; файловая навигация отложена.

При подготовке UI-тест обнаружил неправильную доставку snapshot и ограничение native placeholder для многострочного Text в Windows; оба исправлены. Live-тест теперь ждёт именно готовую кнопку ↑ и проверяет два серверных ответа, а не промежуточное состояние после Stop.

Известные сообщения хостовой EDT: отсутствие `wmic` в Windows 11, дублирующееся расширение ServersView, working-set filter Resource Navigator. Они не являются ошибками Java-кода плагина. Tycho предупреждает о p2-зависимостях в consumer POM; проверка OSGi/p2 выполняется отдельно.

При ошибке смотрите **Window → Show View → Error Log** и `.runtime/.../edt.log`. Не исправляйте её включением полного доступа. Глобальные Codex config/MCP/Skills не меняются. Следующий этап до отдельного решения автора не начинается.


## Диагностика restore для v0.3.1

Проверен исходный журнал `.runtime/development/workspace/.metadata/.log`, в том числе сессии **11.09.2026 19:33:02 и 19:47:02** (EDT 2026.1.3.25, Java 17.0.18). Фактическая рабочая область находится внутри `C:\projects\codex-edt\.runtime`, а не в соседней `codex-edt.runtime`. В этих сессиях **нет записи с первичным исключением нашего bundle**, класса `CodexView`, `Caused by` или строки нашего Java-кода, объясняющей Error Part. Ошибки WMIC, stylesheet EDT и navigator относятся к другим bundle и не объявлены причиной дефекта.

Предоставленные строки сопоставлены с [официальным ViewReference.java Eclipse R4_30](https://github.com/eclipse-platform/eclipse.platform.ui/blob/R4_30/bundles/org.eclipse.ui.workbench/Eclipse%20UI/org/eclipse/ui/internal/ViewReference.java#L98). В `org.eclipse.ui.internal.ViewReference.createPart():101` вызывается `createErrorPart()` при **`descriptor == null`**. На строке **115** Eclipse сам создаёт `new Exception()` для Error Part. В этой ветке **нет вызова конструктора CodexView**; дополнительного `Caused by` у созданного таким образом исключения нет. Источник дескриптора — `WorkbenchPage.createViewReferenceForPart():1980`, поиск через `getViewRegistry().find(descId)`; `ViewRegistry.find():211–215` учитывает также ограничения activities. Это подтверждённый механизм данного короткого стека, но причина отсутствия дескриптора в конкретном запуске пока не установлена.

Сохранённая перспектива содержит и текущий id панели, и неактивную ссылку на старый id из начальной версии. Наличие старой ссылки **не доказывает**, что пользователь видел именно её Error Part. Для установления исторической причины потребовался бы полный id из заголовка/сообщения ошибки. Воспроизведение не заменяется удалением `.metadata`, `workbench.xmi` или preferences.

Проверки исходной v0.3 до функциональных изменений:

- Два процесса полного EDT с одной рабочей областью: открытие, штатное закрытие с открытой View, restore. **Текущая View восстановилась**, даже с недоступным Codex executable (`.runtime/edt-restart-baseline2`).
- Копия пользовательской перспективы и копия configuration cache: текущая View создалась. Отдельная проверка отсутствия активного editor в этих копиях не подходит — в сохранённом состоянии присутствует редактор метаданных; это не ошибка Codex.
- Изолированная копия конфигурации и независимый startup-наблюдатель **без зависимости от Codex bundle**: восстановлен класс `io.github.zhumaniezov.codex.edt.ui.CodexView`, дескриптор доступен, bundle ACTIVE, версия `0.3.0.v20260911191850`. Данные наблюдения лежат в игнорируемой `.runtime/restore-observer`; первоначальный workspace не изменялся. Подключений к информационной базе копия не содержит.

**Вывод:** изменения lifecycle ниже проверены, но объявлять первопричину исходного startup failure найденной или дефект окончательно устранённым пока нельзя. Автор подтвердил успешную ручную приёмку hardened lifecycle. Это позволяет выпустить 0.3.1, сохранив неопределённость исторической причины.

## Регрессии v0.3.1

| Набор | Новые проверки |
|---|---|
| LifecycleViewTest — 12 случаев | Конструктор без вызова фабрики, UI до connection, отсутствующие window/page/editor/project, недоступный executable, exit процесса, ошибки account/read/model/list/thread/list, сохранение статуса «Требуется вход», синхронная ошибка фабрики с retry, dispose во время initialize |
| DeferredClientTest — 3 | Фоновая фабрика и повтор после ошибки, закрытие во время поиска с освобождением позднего клиента ровно один раз, отмена pending connection и запрет callbacks после close |
| TurnRoutingTest — 1 | A → interrupt → B: поздние started/delta/item/completed/error/turn-completed A отбрасываются, дубликаты завершённого item B не меняют итог |
| IdeViewTest — ещё 1 | Та же гонка в настоящих SWT controls: частичный A остаётся раньше ответа B, B содержит только свой текст; thread пригоден для продолжения |
| ViewCallbackTest — 1 | Старый callback клиента после завершения A, начала B и dispose не меняет B и не обращается к уничтоженным controls |
| RestoreScenario — отдельный двухпроцессный smoke | Настоящее сохранение workbench и проверка восстановленной View, включая отсутствие editor/project и ошибку подключения |

Всего обычных тестов: **51** (33 прежних + 18 новых). Два live-теста запускаются отдельно; обычный Maven показывает 53 test, из них 2 skipped. Итоговые пути результатов и версия артефакта приведены ниже.

## Дополнительная ручная приёмка

| Случай | Действия | Ожидаемый результат |
|---|---|---|
| TC-18 — Restore View after EDT restart | Открыть Codex, закрепить справа; закрыть EDT, НЕ закрывая View; снова запустить ту же workspace | Панель восстановилась автоматически, без Error Part и повторного Show View |
| TC-19 — Startup without active editor | Закрыть все редакторы, оставить Codex View, перезапустить EDT | Панель существует; подключение не требует открытого редактора |
| TC-20 — Startup without project context | В отдельной workspace без проектов оставить View и перезапустить EDT | Панель существует; Send без cwd показывает понятное сообщение; повторное подключение доступно |
| TC-21 — Stop then next turn | Запустить длинный запрос A, нажать «Остановить», после возврата отправки сразу отправить уникальный запрос B | A остаётся частичным в своей позиции; в ответ B не добавляется текст A; thread не меняется |
| TC-22 — Markdown clean turn | Новый чат; выполнить запрос ниже, до него НЕ нажимать Stop | Видны выделение, список, inline code и блок кода в native renderer; проверка не связана с прерванным turn |

Запрос TC-22:

```text
Ответь в Markdown:

1. жирный заголовок;
2. маркированный список;
3. inline code Сообщить("Привет");
4. блок BSL-кода.
```

Для TC-18 повторите запуск с открытым BSL-модулем и без него. Не удаляйте persisted state. Если Error Part повторился, **сначала сохраните полный идентификатор представления из сообщения**, время запуска и запись Error Log; не закрывайте проблемную вкладку до фиксации этих данных. Проверить следует и прежние dirty-buffer сценарии, обе темы и SHA-256 по TC-17. Автор выполнил ручную приёмку v0.3.1 и подтвердил успешный результат.


## Итоговые результаты 0.3.1

- **BUILD SUCCESS** — `clean verify`, 11.09.2026 **20:29:50 (+05:00)**; 53 test, 0 failures, 0 errors, 2 skipped, то есть **51 обычный тест прошёл**. Лог: `.runtime/v031-verified-build-2.log`.
- Полный EDT **2026.1.3.25**: **PASS, tests=51, failures=0**, `.runtime/edt-smoke-20260911203036`.
- Настоящий **Codex CLI 0.153.4**: **PASS, tests=2, failures=0**, `.runtime/edt-smoke-20260911203139`. Обнаруженная сервером модель `gpt-6-astra`; saved-сценарий дал **34 потоковых обновления SWT**. Dirty без выделения, dirty + selection, история/resume, physical cwd и неизменность SHA-256 подтверждены. Собственные процессы завершены.
- Restart в двух отдельных процессах, сохранённая одна workspace: **PASS seed + PASS restore**, `.runtime/edt-restart-final031`. Восстановлен настоящий `CodexView`, без editor/project и с намеренно недоступным executable; Error Part отсутствует.
- Дополнительная компиляция на активных библиотеках установленной EDT и официальный p2 Publisher: **PASS**, `.runtime/local-0.3.1.v20260911203036`. Ресурс русской локализации включён и в этот способ упаковки.
- Maven p2: основной bundle/feature **0.3.1.202609111529**, CommonMark 0.30.0; metadata/artifacts валидны, test bundle и тестовые классы отсутствуют, `messages.properties` присутствует.
- Проверены 93 публикуемых файла: признаков ключей, токенов, паролей, персональных путей и корпоративного брендинга не обнаружено. Временные schema, журналы, копии workspace и диагностический наблюдатель остаются вне Git.

SHA-256 установочного ZIP `io.github.zhumaniezov.codex.edt.repository-0.3.1-SNAPSHOT.zip`:

```text
B87F53FE6D8497872E1AB641023E432B7A04935F782137D9878E71FB17605CA9
```

Ручная приёмка принята. Версия публикуется после повторной сборки с формулировкой «Усилена устойчивость Codex View и изоляция запросов». Исходный baseline — `3e6365b`; первопричина прежнего Error Part не объявляется установленной.

Повторная проверка перед публикацией 0.3.1: 11.09.2026 20:46:32 (+05:00), **BUILD SUCCESS**, 51 обычный тест, 2 live-теста пропущены штатно. Лог `.runtime/v031-publication-build.log`. Проверка p2 и 93 публикуемых файлов прошла; новый qualifier `0.3.1.202609111545`, SHA-256 ZIP: `D3B66505535066429FBE19E5A42BC9DBDEF2991E70BDD1762AF636D18B90A371`.
